import { renderWeek } from '../calendar';
import { renderHeader } from '../header';
import { renderTimescale } from '../timescale';

it('Should render the week', () => {
    const result = renderWeek();
    expect(result).toEqual();
});
it('Should render header', () => {
    const result = renderHeader();
    expect(result).toEqual();
});
it('Should render time scale', () => {
    const result = renderTimescale();
    expect(result).toEqual();
});